import React from "react";

import { YoutubeData } from "../utils/interfaces";

const ExerciseVideos = ({
  exerciseVideos,
  name,
}: {
  exerciseVideos: YoutubeData[];
  name: string;
}) => {
  return <div>ExerciseVideos</div>;
};

export default ExerciseVideos;
